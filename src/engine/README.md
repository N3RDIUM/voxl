# Engine
This is like a tiny game engine in and of itself, built specifically for Voxl.
I decided to keep this as a separate directory from `src/client` because I plan
to eventually reuse it for some other projects.

